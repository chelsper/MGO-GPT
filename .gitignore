FundLayerDemo/

# Workspace dependencies are installed locally and are never source files.
node_modules/

# Local environment files. Keep real credentials in Vercel or a password manager.
.env
.env.*
apps/**/.env
apps/**/.env.*
!.env.example
!apps/**/.env.example
