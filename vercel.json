{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "react-router",
  "regions": ["iad1"],
  "installCommand": "cd apps/web && npm ci --no-audit --no-fund",
  "buildCommand": "cd apps/web && npm run build",
  "crons": [
    {
      "path": "/api/internal/report-cache-refresh",
      "schedule": "0 * * * *"
    }
  ]
}
